PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# El Path es deducido automáticamente del directorio raíz del proyecto; sobrescribir solo si es necesario
# METRICS_DATA_PATH=/absolute/path/to/metrics.json
#! Se sabe perfectamente que jamás se debe incluir el archivo .env en el repositorio, pero como se trata de un proyecto
# correspondiente a una prueba técnica, se deja disponible para facilitar la ejecución del proyecto a quien lo revise.